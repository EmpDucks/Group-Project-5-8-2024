radius = float(input("What is the radius of the circle? (Type numbers only, don't include a measurement)"))
# the "float" function converts the input into a floating-point number (or a number with a decimal) used to convert numbers and strings
area = radius * int(3.141519)
# This line is used to calculate the are. it calculates the inputted number by pi.
print(f"The area of the circle is ~{area}")
# print results.